name: hello-world

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: self-hosted

    steps:
      - name: Show machine info
        run: |
          echo "Hello World!"
          echo "This job is running on:"
          hostname

      - name: Check GPU
        run: |
          nvidia-smi || echo "No GPU detected"
